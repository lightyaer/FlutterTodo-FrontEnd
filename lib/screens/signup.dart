import 'package:flutter/material.dart';

class SignupScreen extends StatelessWidget {
  static String tag = "page-signup";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("SignupScreen"),
    );
  }
}
