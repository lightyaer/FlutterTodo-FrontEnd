import 'package:flutter/material.dart';

class TodoScreen extends StatelessWidget {
  static String tag = "page-todo";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("TodoScreen"),
    );
  }
}
